#include <cstdio>

unsigned long long MIN, MAX;

unsigned long long* arr;

unsigned long long calculate(const unsigned long long& num);

int main() {
	scanf("%llu %llu", &MIN, &MAX);

	arr = new unsigned long long[MAX - MIN + 1];

	return 0;
}

unsigned long long calculate(const unsigned long long& num) {

};